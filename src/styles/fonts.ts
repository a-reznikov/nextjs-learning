import { Open_Sans } from "next/font/google";

const openSans400 = Open_Sans({
  subsets: ["latin"],
  weight: "400",
});

const openSans600 = Open_Sans({
  subsets: ["latin"],
  weight: "600",
});

export { openSans400, openSans600 };
