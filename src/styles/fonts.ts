import { Open_Sans } from "next/font/google";

const OPEN_SANS_400 = Open_Sans({
  subsets: ["latin"],
  weight: "400",
});

const OPEN_SANS_600 = Open_Sans({
  subsets: ["latin"],
  weight: "600",
});

export { OPEN_SANS_400, OPEN_SANS_600 };
